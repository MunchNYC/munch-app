import 'package:flutter/material.dart';

class BottomTabWdiget extends StatelessWidget {
  final Color color;

  BottomTabWdiget(this.color);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: color,
    );
  }
}
}
